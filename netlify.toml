[build]
# base = "/"
command = "npm run build"
publish = "build"

[dev]
targetPort = 5173
command = "npm run dev"
framework = "#custom"
publish = "src"

[functions]
directory = "functions"
# node_bundler = "esbuild"
